import type { ContactConfig } from '@/lib/types';

export const contactConfig: ContactConfig = {
  title: 'Pide tu Cita',
  subtitle:
    '¿Tienes alguna pregunta o quieres reservar una cita? Escríbenos y te responderemos en menos de 24 horas.',
  formEndpoint: '',
};
